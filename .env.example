# Mercado Pago Backend - Environment Variables
# Copy this file to .env and fill the values

# Required: Access token from Mercado Pago (use Test credentials for dev)
MP_ACCESS_TOKEN=YOUR_MP_ACCESS_TOKEN

# Optional: Public base URL of your app (e.g., https://mp-backend.onrender.com)
APP_BASE_URL=

# CORS allowlist: comma-separated origins. Use * only for testing
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Optional: Explicit webhook URL (fallback is APP_BASE_URL + /webhooks/mercadopago)
MP_WEBHOOK_URL=

# PORT is provided by Render automatically; keep for local dev
PORT=3000
